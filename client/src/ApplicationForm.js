import React from "react";
import styled from "styled-components";

const ApplicationForm = ({ job }) => {
  return <Wrapper>THIS IS THE Applications FORM</Wrapper>;
};

export default ApplicationForm;

const Wrapper = styled.div``;
